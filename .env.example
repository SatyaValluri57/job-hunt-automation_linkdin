# Copy this file to .env and fill in your values. Never commit the real .env file.

# Telegram notifications (optional) — daily summaries + two-way approve/edit/skip for
# low-confidence form fields. See smart_apply/telegram_notifier.py for setup steps:
#   1. Message @BotFather on Telegram -> create bot -> get TELEGRAM_BOT_TOKEN
#   2. Message your bot once -> get your TELEGRAM_CHAT_ID
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Groq AI (optional) — used for AI-assisted field answers and job match scoring
# when useGroqAI is enabled in config.py
GROQ_API_KEY=
